title = "Conxian Gateway gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Non-secret compressed public key literals used in tests and API payload examples"
regexes = [
  '''(?i)public_key.{0,64}0[23][0-9a-f]{64}''',
  '''0[23][0-9a-f]{64}''',
]
