title = "Conxian Gateway gitleaks configuration"

[extend]
useDefault = true

# Allow generic-api-key in test fixture files (field names like "device_key" match the pattern)
[[allowlist]]
description = "Allow generic-api-key in test fixture files"
file = "cmd/gateway/tests/.*\\.rs"
