title = "Conxian Gateway gitleaks configuration"

[extend]
useDefault = true

# Allow generic-api-key rule in test files (false positives on fixture field names)
[[rules.allowlist]]
description = "Allow generic-api-key in test fixtures"
rule = "generic-api-key"
path = "cmd/gateway/tests/"
