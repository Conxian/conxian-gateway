title = "Conxian Gateway gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Allow the existing test-suite exception and the one known synthetic Groth16 fixture; neighboring fixture files remain scanned."
paths = [
    "cmd/gateway/tests/",
    '''^internal/engine/tests/fixtures/groth16/bitvm_fixture\.json$''',
]
