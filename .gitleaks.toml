title = "Conxian Gateway gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Allow generic-api-key findings in synthetic test fixture files"
paths = [
    "cmd/gateway/tests/",
    "internal/engine/tests/fixtures/groth16/",
]
